import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'space-between',
  },
  field: {
    marginTop: 16,
  },
  button: {
    marginTop: 24,
  },
  switch: {
    marginTop: 24,
  }
});

export { styles };