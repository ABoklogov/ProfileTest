import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 43,
  },
  photo: {
    borderRadius: '50%',
    marginHorizontal: 23,
  },
});

export { styles };